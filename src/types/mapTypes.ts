export type MapConfig = {
    avatar: string;
    maxZoom: number;
    minZoom: number;
    style: string;
    accessToken: string;
    title: string;
    type?: string;
  };
  
  export type LocationType = {
    lat: number;
    lng: number;
  };
  